
export interface Assigment {
  account: {
    id: string
    code: string
    name: string
  }
  role: {
    id: number
    name: string
  }
  unit: {
    id: number
    name: string
  }
}
