export const {
  PORT,
  DB_HOST,
  DB_PORT,
  DB_DATABASE,
  DB_USERNAME,
  DB_PASSWORD,
  DB_SCHEMA,
  NODE_ENV,
  G_CLIENT_ID,
  G_CLIENT_SECRET,
  SECRET_KEY,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  AWS_SESSION_TOKEN,
  BUCKET_NAME,
  BUCKET_REGION,
} = process.env
