import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_auth/matricula/distribucion')({
  component: () => <div>Hello /_auth/matricula/distribucion!</div>,
})
